kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mapboxcache-hostpath-pvc
  namespace: sip-prod
spec:
  storageClassName: microk8s-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi