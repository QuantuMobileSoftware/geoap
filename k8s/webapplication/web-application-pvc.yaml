apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sip-data-volumeclaim
  namespace: sip
spec:
  # storageClassName: standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi