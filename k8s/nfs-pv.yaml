apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-sip-data
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 192.168.1.58
    path: "/volume1/SIP"
    readOnly: false
