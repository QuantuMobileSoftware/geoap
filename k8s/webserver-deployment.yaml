apiVersion: v1
kind: Service
metadata:
  name: webserver
  namespace: sip-prod
  labels:
    app: sip
spec:
#  type: LoadBalancer
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort:  31080
      protocol: TCP
      name: http
  selector:
    app: sip
    tier: frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: sip-prod
  labels:
    app: sip
    tier: frontend
spec:
  selector:
    matchLabels:
      app: sip
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: sip
        tier: frontend
    spec:
      containers:
        - name: frontend
          image: 192.168.1.34:5000/sip-webserver:latest
          imagePullPolicy: "Always"
          envFrom:
            - configMapRef:
                name: webserver-config
          ports:
            - containerPort: 80
              name: frontend
          volumeMounts:
            - name: data-persistent-storage
              mountPath: /data
            - name: mapboxcache-persistent-storage
              mountPath: /data/mapboxcache
      volumes:
        - name: data-persistent-storage
          persistentVolumeClaim:
            claimName: nfs-sip-data-pvc
        - name: mapboxcache-persistent-storage
          persistentVolumeClaim:
            claimName: mapboxcache-hostpath-pvc
      imagePullSecrets:
        - name: regcred
