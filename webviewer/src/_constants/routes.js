const ROOT = '/';
const AUTH = `${ROOT}auth`;
const SIGN_UP = `${ROOT}signup`;
const SIGN_UP_CONFIRM = `${ROOT}signup/account-confirm-email/:id`;

export const ROUTES = { ROOT, AUTH, SIGN_UP, SIGN_UP_CONFIRM };
