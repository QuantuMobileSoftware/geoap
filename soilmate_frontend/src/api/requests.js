import { areasRequests } from './areas';
import { authRequests } from './auth';
import { usersRequests } from './users';

export const API = {
  areas: areasRequests,
  auth: authRequests,
  users: usersRequests
};
